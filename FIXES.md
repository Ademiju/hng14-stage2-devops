# FIXES

This file lists the important fixes that materially changed the application’s behavior, reliability, security, or delivery process.

| File | Line(s) | Problem | Change |
| --- | --- | --- | --- |
| `api/main.py` | 10-44 | The API was tied to local-only Redis settings, used an inconsistent queue name, returned weak HTTP semantics, and had no health endpoint. | Switched Redis and queue configuration to environment variables, returned `201` on job creation, returned proper `404` errors for missing jobs, and added `/health`. |
| `worker/worker.py` | 7-47 | The worker assumed local Redis, had no configurable runtime settings, skipped the `processing` state, and did not shut down cleanly. | Made Redis and queue settings environment-driven, added `WORKER_POLL_TIMEOUT` and `JOB_DURATION_SECONDS`, introduced the `processing` status, and added signal-based shutdown handling. |
| `frontend/app.js` | 6-52 | The frontend was hardcoded to `localhost:8000`, masked upstream API errors, had no request timeouts, and lacked an application health endpoint. | Made the API URL and bind settings configurable, preserved upstream status codes when possible, added request timeouts, and exposed `/health`. |
| `frontend/views/index.html` | 21-68 | The UI assumed successful backend responses, handled polling poorly, and contained a visible encoding issue. | Added submission and polling error handling, stopped polling on terminal states, and replaced the broken dash rendering with a safe ASCII separator. |
| `api/.env` | 1-2 | A Redis password was committed in source control. | Removed the tracked secret, added `api/.env.example`, and kept real env files out of version control. |
| `.env.example` | 1-59 | The stack had no complete runtime template for Compose configuration. | Added a full root env template covering service images, ports, limits, health checks, queue settings, and network/volume names. |
| `.gitignore` | 1-11 | Local env files, caches, and generated artifacts were not being ignored consistently. | Added ignore rules for env files, Python cache files, coverage output, and `node_modules`, while preserving `*.env.example`. |
| `api/Dockerfile`, `worker/Dockerfile`, `frontend/Dockerfile` | 1-15, 1-13, 1-12 | The services had no production container build definitions. | Added Dockerfiles for all three services so the stack can be built and run consistently. |
| `api/.dockerignore`, `worker/.dockerignore`, `frontend/.dockerignore` | 1-6, 1-6, 1-3 | Build contexts could include local env files, virtualenvs, caches, or `node_modules`. | Added `.dockerignore` files to keep image builds clean and predictable. |
| `docker-compose.yml` | 1-125 | The repository had no production-style orchestration and later needed fixes for health-gated startup, internal networking, named persistence, and variable-driven configuration. | Added a Compose stack for all services, kept Redis internal-only, wired health-based dependency ordering, used an internal named network, and attached Redis persistence through a named volume. |
| `api/__init__.py`, `api/tests/conftest.py`, `api/tests/test_main.py` | 1, 1-5, 1-56 | The repo had no API unit tests, and package imports were brittle in CI. | Added mocked-Redis pytest coverage for the API and ensured imports resolve consistently during test collection. |
| `api/requirements-dev.txt`, `worker/requirements-dev.txt` | 1-5, 1-2 | Fresh local environments could fail with missing Python modules during development, testing, or linting. | Added dev requirements files so API and worker dependencies can be installed explicitly in one step. |
| `frontend/package.json`, `frontend/.eslintrc.json`, `.flake8` | 1-15, 1-10, 1-3 | The repo did not include the local configuration needed for the required lint stages. | Added ESLint configuration and script for the frontend plus Flake8 configuration for the Python services. |
| `.github/workflows/ci-cd.yml` | 1-274 | There was no CI/CD pipeline enforcing quality gates, image builds, security scanning, integration testing, and controlled deployment. | Added a GitHub Actions pipeline on `ubuntu-latest` that runs `lint -> test -> build -> security scan -> integration test -> deploy` in strict order. |
| `scripts/integration_test.sh` | 1-58 | The integration flow was missing and then required fixes for Compose env loading, container-local frontend access, clean teardown, and BusyBox-compatible POST requests. | Added a full-stack integration test script that brings the stack up, exercises the frontend job flow, polls to completion, and always tears the stack down. |
| `scripts/rolling_update.sh` | 1-148 | Deployment needed a scripted rolling update that keeps the old container running if the candidate never becomes healthy. | Added a rolling deployment script that waits for candidate health, swaps only after success, and aborts without stopping the old container if health does not pass within the timeout. |
| `README.md` | 1-101 | The documentation no longer matched the application once Compose, local dev requirements, and CI/CD were added. | Rewrote the README to reflect the current services, local setup, Docker workflow, smoke tests, and GitHub Actions pipeline behavior. |
